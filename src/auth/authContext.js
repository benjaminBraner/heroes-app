import { createContext } from "react";

//el nombre esta capitalizado porque en pocas palabras es un componente, porque el createContext genera un componenente
export const  AuthContext = createContext();
